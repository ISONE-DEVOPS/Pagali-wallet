name: pagali_wallet
description: Pagali — Cape Verde P2P + P2M digital wallet.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  intl: ^0.19.0
  http: ^1.2.0
  shared_preferences: ^2.2.2
  crypto: ^3.0.3
  uuid: ^4.3.3
  mobile_scanner: ^7.0.0
  qr_flutter: ^4.1.0
  share_plus: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/
